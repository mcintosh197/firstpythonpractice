hello = "hello" .upper ()
print (hello)

# print (type(hello)) do this to find the type.
# .upper () this is putting the string in all capitals.
# print (hello.upper )
# Opposite with .lower () or print (hello.lower)
# .capitalize for the first letter in the string.
# .count it's counting a sepcific string within the string.
# .count for example if you wanted to see how many double L then use print (hello.count (LL)).
# It's important that when using count to use the exact same syntex.

# String multiplication and addition
x = "hello"
y = 3

print (x * y)
# This will give you the string multiple times because it's multipled by an integer.