# This is a list which is an ordered collection.
x = [4, True, "hi"]
x.append ("hello")
x.extend([4,5,6,7,8,])
x.pop() #This will remove the last item from the list.
x.pop (0) #This will remove the first thing on the list
print(len(x))
print(x)

# Tuple is round brackets not square and cannot be changed.
y = (0,1,2,3,4,5)
print(y)